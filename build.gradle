apply plugin: 'java'

dependencies {
    compile project(path: ':CommonUtils')
    compile project(path: ':CommonFeed')
}

repositories {
    flatDir {
        dirs 'libs'
    }
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"